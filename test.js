function calculate() {
    var entry = "1*2+3/4";
    console.log(entry.length -1)
    // var arr = entry.split(/(\D)/g);
    // console.log(arr)
    // var nt = Number(arr[0]);
    // for (i = 0; i < arr.length; i++) {
    //   var nextNumber = Number(arr[i+1])
    //   var symbol = arr[i];
    //
    //   if (symbol === '+') { nt += nextNum; }
    //   else if (symbol === '-') { nt -= nextNum; }
    //   else if (symbol === '*') { nt *= nextNum; }
    //   else if (symbol === '/') { nt /= nextNum; }
    //   i++
    //   console.log(nt)
    // }
}
calculate()
